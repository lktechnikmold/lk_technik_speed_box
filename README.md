# lk_technik_speed_box
Arduino-based GPS speedbox that reads NMEA data via serial and outputs a precise speed signal (130 pulses/meter) on a 7-pin connector. Compensates for wheel slip using RTK GPS. Auto-detects baud rate. Built around Arduino Nano + ULN2803 Darlington array
